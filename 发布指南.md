# 发布指南（傻瓜版）

本文件夹 `ibka-balance-plugin` 就是可以直接上传的插件包。你要做的只是：**登录 GitHub → 建一个公开仓库 → 把这些文件拖进去**。

## 第一步：登录 GitHub

1. 打开 https://github.com/login ，登录你的账号（git 里配置的是 phonejoy）。
2. 没有账号就先注册（免费，邮箱即可）。

## 第二步：建仓库

1. 打开 https://github.com/new
2. 仓库名填：`dsh-ibka-balance`
3. 选 **Public**（公开，别人才能看到和安装）
4. 其他不用动，点绿色按钮 **Create repository**

## 第三步：上传文件（不用懂 git）

1. 在新仓库页面，点 **uploading an existing file**（上传已有文件）
2. 把本文件夹里的这些文件拖进去：
   - `package.json`
   - `README.md`
   - `LICENSE`
   - `.gitignore`
   - 整个 `lib` 文件夹（里面有 `index.js` 和 `client.js`）
3. 点 **Commit changes**（提交）

完成！你的插件就有公开地址了：`https://github.com/你的名字/dsh-ibka-balance`

## 第四步（可选）：进社区插件目录

把仓库地址发给 AI 助手（或者我），它会帮你起草给 [awesome-dsh-plugin](https://github.com/awesome-dsh-plugin/awesome-dsh-plugin) 的收录申请（PR），让更多 DSH 用户能看到你的插件。

## 备选路线：让我用命令行帮你推

如果你愿意，也可以：
1. 安装 GitHub 官方命令行工具 `gh`（Mac 上运行 `brew install gh` 或官方安装脚本）；
2. 运行 `gh auth login` 完成一次登录（浏览器授权）；
3. 然后告诉我"开始吧"，我就能帮你建仓库、推代码、提 PR，全程自动。

二选一即可，推荐第一条（不用装任何东西）。
